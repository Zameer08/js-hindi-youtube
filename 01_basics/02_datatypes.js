"use strict"; // treat all JS code as newer version

// alert( 3 + 3) // we are using nodejs, not browser. we can also use the console of the brower enginer it will give answer of it instead of it because js engine is tied to browsr engines

console.log(3 
    +
     3) // code readability should be high

console.log("Hitesh")


let name = "Hashira"
let age = 18
let isLoggedIn = false
let state;

// number => 2 to power 53
// bigint
// string => ""
// boolean => true/false
// null => standalone value
// undefined => 
// symbol => unique


// object

console.log(typeof undefined); // undefined
console.log(typeof null); // object

/* Remember this that the type of undefined is undefined which is a type itself in it but null is showed or appearead as an object on the terminal, there is a lot of debate which says it is an error in the language and some dont oppose it at all and reply that it is a part of the language that null should be an object*/